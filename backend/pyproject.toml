[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "*/migrations/*",
    "scratch/*",
    "staticfiles/*",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
